cmake_minimum_required(VERSION 2.6)
project(Test)
set(VTK_DIR "/home/guillaume/source/vtk/build")
find_package(VTK REQUIRED)
#include(${VTK_USE_FILE})
add_executable(edl edl.cxx)
target_link_libraries(edl ${VTK_LIBRARIES})
